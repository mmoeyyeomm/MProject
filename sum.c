#include<stdio.h>

void main()
{
	int m, n, a, b, c, d;
	float score, total, average;
	scanf("%d %d %d %d", &a, &b, &c, &d);
	for(n = 1; n <= a, b, c, d; n++)
	{
			total = 0;
			printf("student %d\n", n);
			for(m = 1; m <= a + b + c + d; m++)
	{
		
		scanf("%f", &score);
		printf("%f", score);
		total = total + score;
	}		
			average = total;
			printf("\nThe average of student %d is %f n",n,average);
	}
			
}

